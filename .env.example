# Kalshi Research Platform - Environment Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# API ENVIRONMENT (OPTIONAL)
# ============================================================================
# The CLI defaults to `prod` if KALSHI_ENVIRONMENT is unset (or if you pass no
# global `--env`). For safer experimentation, use `demo`.
# Invalid values will cause the CLI to exit with an error (to avoid accidental prod usage).
# Demo: https://demo-api.kalshi.co (paper trading, no real money)
# Prod: https://api.elections.kalshi.com (real trading)
KALSHI_ENVIRONMENT=demo

# ============================================================================
# AUTHENTICATION (OPTIONAL)
# ============================================================================
# Public endpoints work WITHOUT credentials.
# Auth is only needed for authenticated CLI commands (e.g., `kalshi portfolio ...`).
# Get API keys at: https://kalshi.com/api

# Your Kalshi key id (leave empty for public-only access)
KALSHI_KEY_ID=

# Provide ONE of the following private key inputs:
KALSHI_PRIVATE_KEY_PATH=
# OR (for CI/environments without filesystem access):
# KALSHI_PRIVATE_KEY_B64=

# Optional (recommended): separate demo credentials.
# When KALSHI_ENVIRONMENT=demo, these take precedence over the non-prefixed vars.
KALSHI_DEMO_KEY_ID=
KALSHI_DEMO_PRIVATE_KEY_PATH=
# OR (for CI/environments without filesystem access):
# KALSHI_DEMO_PRIVATE_KEY_B64=

# CI helper (macOS): base64 encode your private key content without newlines:
#   base64 -i your_key.pem | tr -d '\n'

# ============================================================================
# RATE LIMITS (OPTIONAL)
# ============================================================================
# Only affects authenticated calls (portfolio commands). Public endpoints are not write-rate-limited.
# Options: basic (20r/10w), advanced (30r/30w), premier (100r/100w), prime (400r/400w)
KALSHI_RATE_TIER=basic

# ============================================================================
# LOGGING (OPTIONAL)
# ============================================================================
# Structured log level for CLI runs (logs go to stderr; CLI stdout stays parseable).
# Examples: DEBUG, INFO, WARNING, ERROR
# KALSHI_LOG_LEVEL=WARNING

# ============================================================================
# EXA (OPTIONAL) - AI-POWERED RESEARCH + NEWS
# ============================================================================
# Required to use Exa-powered commands:
# - `kalshi research context|topic|similar|deep`
# - `kalshi research thesis create --with-research`
# - `kalshi research thesis check-invalidation|suggest`
# - `kalshi news ...`
EXA_API_KEY=

# Optional tuning:
# EXA_BASE_URL=https://api.exa.ai
# EXA_TIMEOUT=30
# EXA_MAX_RETRIES=3
# EXA_RETRY_DELAY=1

# ============================================================================
# TESTS (OPTIONAL)
# ============================================================================
# Enables live API integration tests (skipped by default).
# KALSHI_RUN_LIVE_API=1
